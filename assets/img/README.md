# Images

Put the hero portrait here as **`portrait.jpg`**.

The hero frame (`.portrait` in `index.html`) loads `assets/img/portrait.jpg`
and crops it square (`cover`, centered). Until that file exists the frame
shows an empty outline rather than a broken image.

Keep it reasonably sized for the web — roughly 800px on the long edge is
plenty at the size it renders.
